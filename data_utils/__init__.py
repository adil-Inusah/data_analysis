"""Compatibility wrappers for the project data utilities.

These modules re-export the existing analysis helpers from the src/ folder so the
repo can expose a cleaner, more organized package structure without breaking older
entry points.
"""
